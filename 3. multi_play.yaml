#playbook is list of plays
- name: Play-1
  hosts: localhost
  tasks: 
  - name: paly-1 and task 1
    ansible.builtin.debug:
      msg: "Hello from play 1 and task 1"

- name: Play-2
  hosts: localhost
  tasks: 
  - name: paly-2 and task 2
    ansible.builtin.debug:
      msg: "Hello from play 2 and task 2"
